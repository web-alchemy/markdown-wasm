{
  "scripts": {
    "build": "wasm-pack build --target nodejs --scope web-alchemy",
    "test": "node --test",
    "pack": "wasm-pack pack",
    "publish": "wasm-pack publish --access=public"
  },
  "devDependencies": {
    "wasm-pack": "^0.14.0"
  },
  "overrides": {
    "tar": "7.5.11",
    "axios": "0.30.0",
    "minimatch": "3.1.3"
  }
}
